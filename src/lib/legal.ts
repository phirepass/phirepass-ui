/**
 * Revision date of the terms and privacy documents.
 *
 * One constant so the date printed on the page and the `lastmod` in the sitemap
 * can never disagree — a sitemap that claims a legal document changed today,
 * every day, teaches crawlers to ignore the field. Bump this whenever either
 * document is edited in substance.
 */
export const LEGAL_UPDATED_ISO = "2026-08-14";

/** The same date, as it is printed for the reader. */
export const LEGAL_UPDATED_LABEL = "14 August 2026";
